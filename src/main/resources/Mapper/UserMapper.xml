<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.paperwise.Mapper.UserMapper">
    <select id="getAllUsers" resultType="org.example.paperwise.entry.User">
        select * from user
    </select>
    <insert id="addUser" useGeneratedKeys="true" keyColumn="userid" keyProperty="userid">
        insert INTO user(username,password,email)values (#{username},#{password},#{email})
    </insert>

    <select id="getUserByUserName" resultType="org.example.paperwise.entry.User">
        select * from user where username=#{username}
    </select>


    <select id="getUserById" resultType="org.example.paperwise.entry.User">
        select * from user where userid=#{userid}
    </select>
</mapper>